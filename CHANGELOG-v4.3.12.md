# DigiAhan CDR v4.3.12

## Seller workspace

- تماس واقعی فروشنده با Server-Sent Events و fallback دوره‌ای نمایش داده می‌شود؛ بازگشت به تب نیز فوراً وضعیت را کنترل می‌کند.
- همه داده‌های نمایشی آریا سازه، حسنا، تیرآهن ۱۶ و ۲۰ تن از صفحه عملیاتی حذف شدند.
- فرم نتیجه مکالمه از نو طراحی شد: فقط نتیجه اصلی الزامی است؛ محصول، مقدار و اقدام‌های فروش اختیاری‌اند.
- تماس غیر فروش، شماره اشتباه، ارجاع به واحد دیگر و تماس شخصی/همکار بدون اجبار به ثبت محصول ذخیره می‌شوند.
- شماره‌های فارسی، عربی، `+98`، `0098` و شماره بدون صفر به قالب استاندارد تبدیل می‌شوند و خطاهای اعتبارسنجی فارسی‌اند.
- تماس‌های بی‌نتیجه امروز قابل بازکردن و ثبت هستند؛ تعامل‌های خود فروشنده از Timeline قابل اصلاح‌اند.
- Timeline مشتری دیگر محدود به شش ماه نیست، همه شماره‌های هویت مرکزی را پوشش می‌دهد و فاکتورهای حسابداری را نیز در ترتیب زمانی نشان می‌دهد.
- جست‌وجوی مشتری از اولین نویسه، چندکلمه‌ای و مستقل از ترتیب بخش‌های نام اجرا می‌شود و شماره‌های موبایل را نمایش می‌دهد.

## Dashboard and AI

- نشانگر دریافت گزارش دیگر کل صفحه را قفل نمی‌کند؛ داده قبلی هنگام تازه‌سازی قابل استفاده است.
- نسخه برنامه در بالای هر ۹ صفحه از `/api/version` نمایش داده می‌شود.
- صفحه AI دیگر در خطای اتصال، داده نمونه را به‌عنوان داده عملیاتی جایگزین نمی‌کند؛ حالت نمونه فقط با `?demo=1` فعال است.
- وضعیت نبود فایل تنظیم دریافت ضبط Issabel در صفحه AI صریح نمایش داده می‌شود.
- ابزار `CONFIGURE-AI-v4.3.12.ps1` برای فعال‌سازی امن SFTP و Faster Whisper بدون قرار دادن رمز/کلید در بسته افزوده شد.

## Regression

- Build Release: صفر خطا و صفر هشدار.
- Smoke test شماره تلفن‌های فارسی، بین‌المللی و بدون صفر: موفق.
- JavaScript syntax check: موفق.
- Seller UI regression: موفق؛ ۹ صفحه و نبود داده نمایشی/پیش‌فرض کنترل شد.
- تست Migration SQL/AI در محیط Codex به‌علت نبود مجوز SSPI/SQL Login اجرا نشد؛ Migrationها در این نسخه تغییری نکرده‌اند.
