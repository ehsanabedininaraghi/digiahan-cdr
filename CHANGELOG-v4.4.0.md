# DigiAhan CDR v4.4.0 — Customer Journey Kernel

## قابلیت‌های جدید

- هسته «هیچ مشتری گم نشود» بر پایه `IdentityId` اضافه شد.
- سرنخ، فرصت فروش، تاریخچه مرحله، صف کار، SLA، Journey Event و Transactional Outbox اضافه شدند.
- رابط مستقل فروشنده در `/seller-v3` بدون لینک به داشبورد مدیریت ساخته شد.
- مرکز کنترل موارد عبورکرده از SLA در `/journey-control` و پشت رمز مدیریت ساخته شد.
- ساخت سرنخ، تبدیل به فرصت، تغییر Pipeline و تکمیل اقدام همگی idempotent و مالک‌محور هستند.
- اتصال اختیاری و best-effort نتیجه Seller v2 به هسته جدید آماده شد.

## سازگاری و ایمنی

- Feature Flag پیش‌فرض خاموش است.
- فعال‌سازی نخستین پایلوت بدون `SellerKey` مشخص رد می‌شود.
- capture خودکار Seller v2 سوییچ جدا دارد و پیش‌فرض خاموش است.
- migration فقط افزایشی و قابل اجرای دوباره است؛ `DROP`، `TRUNCATE` یا حذف داده مشتری ندارد.
- نصب‌کننده قبل از جایگزینی backup می‌گیرد، نسخه را build و health-check می‌کند و در خطا rollback خودکار انجام می‌دهد.
- rollback دستی فایل‌های جدید را به قرنطینه قابل‌بازیابی منتقل می‌کند و جداول افزایشی را حذف نمی‌کند.

## تست‌ها

- Build Release: موفق، صفر خطای کامپایل.
- Smoke قواعد Journey و نسخه‌های قبلی: موفق.
- JavaScript syntax: موفق.
- UI regression: موفق؛ ۱۱ صفحه، نبود لینک مدیریت در Seller v3 و کامل‌بودن IDهای UI کنترل شد.
- Migration static safety/idempotency: موفق.
- تست اجرای migration روی SQL موقت به‌صورت پروژه مستقل اضافه شد و برای اجرا نیازمند connection string صریح تست است؛ روی دیتابیس کاری اجرا نشده است.

## نکته انتشار

این نسخه برای نصب خاموش و پایلوت کنترل‌شده آماده می‌شود. اجرای مستقیم migration یا فعال‌سازی عمومی روی دیتابیس تولید در این مرحله مجاز نیست.
