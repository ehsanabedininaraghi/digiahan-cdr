# DigiAhan CDR v4.4.0 — Customer Journey Kernel

## قابلیت‌های جدید

- هسته «هیچ مشتری گم نشود» بر پایه `IdentityId` اضافه شد.
- سرنخ، فرصت فروش، تاریخچه مرحله، صف کار، SLA، Journey Event و Transactional Outbox اضافه شدند.
- میزکار مستقل فروشنده در `/seller-v3` بدون لینک به داشبورد مدیریت ساخته شد.
- مرکز کنترل موارد عبورکرده از SLA در `/journey-control` و پشت رمز مدیریت قرار گرفت.
- ایجاد سرنخ، تبدیل به فرصت، تغییر مرحله و تکمیل اقدام بعدی idempotent و مالک‌محور هستند.
- اتصال اختیاری و best-effort نتیجه Seller v2 به Journey آماده شد؛ خطای Journey پاسخ Seller v2 را خراب نمی‌کند.

## سازگاری و ایمنی نصب

- Feature Flag و Auto Capture به‌صورت پیش‌فرض خاموش هستند و Seller v2 مسیر فعال تولید باقی می‌ماند.
- migration فقط افزایشی، قابل اجرای مجدد و بدون حذف داده است.
- `global.json` داخل payload قرار گرفت تا نصب همیشه با SDK 8 ساخته شود و به SDK ناقص جدیدتر منحرف نشود.
- نصب‌کننده پیش از توقف برنامه یا کپی فایل، مسیر و سلامت Windows Service را کنترل می‌کند.
- اگر سرویس به پوشه دیگری اشاره کند یا هویت سرویس به SQL دسترسی نداشته باشد، نصب بدون هیچ تغییری متوقف می‌شود.
- توقف پردازش‌های هم‌نام فقط به listener سامانه و executable داخل همان Repository محدود شد.
- نصب‌کننده حالت `-ValidatePackageOnly` دارد که بدون توقف سرویس یا دست‌زدن به دیتابیس، payload را کنترل می‌کند.
- backup، rollback خودکار و rollback دستی قرنطینه‌ای حفظ شدند.

## تست‌های انجام‌شده

- Build Release برنامه و هر دو پروژه تست: موفق، صفر خطا و صفر هشدار.
- Smoke test قوانین Journey و سازگاری نسخه‌های قبلی: موفق.
- UI regression روی ۱۱ صفحه و syntax همه JavaScriptها: موفق.
- اجرای HTTP ایزوله روی پورت 5099: نسخه، Seller v3 و حفاظت صفحه مدیریت موفق.
- migration دوبار پشت‌سرهم روی SQL موقت: موفق.
- گردش کامل Repository روی SQL موقت: ایجاد/idempotency سرنخ، تبدیل به فرصت، تغییر مرحله، کار بعدی، فروش موفق، SLA و Outbox موفق.
- پل Seller v2 و جلوگیری از رکورد تکراری: موفق.
- دیتابیس موقت بعد از تست حذف و عدم باقی‌ماندن آن کنترل شد.
- ZIP، تمام SHA-256ها، پوشش Manifest، نبود فایل محرمانه/محلی و حالت Validate-only نصب‌کننده کنترل می‌شوند.

## نکته انتشار

نسخه برای نصب خاموش و پایلوت کنترل‌شده ساخته شده است. فعال‌سازی Journey فقط بعد از نصب سالم و برای SellerKeyهای مشخص انجام شود.
