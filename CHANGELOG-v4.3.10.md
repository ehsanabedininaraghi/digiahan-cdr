# DigiAhan CDR v4.3.10

## اصلاح‌های اصلی

- یکسان‌سازی ساعت‌ها بر پایه UTC در دیتابیس و نمایش شمسی تهران در رابط کاربری.
- استفاده از `ReceivedAtUtc` برای گزارش تماس‌ها تا ساعت‌های آینده و نادرست Issabel وارد داشبورد نشوند.
- نگهداری تماس جاری در SQL و بازیابی آن پس از F5 یا از دست رفتن حافظه موقت برنامه.
- جست‌وجوی تدریجی مشتری با نام، شرکت و شماره و نمایش همه موبایل‌های مرتبط.
- نمایش نتیجه جست‌وجوی مشتری به‌صورت فقط‌خواندنی؛ ثبت تعامل فقط برای تماس واقعی یا مورد ناقص مجاز است.
- ورود مستقل فروشندگان با جدول کاربران، PBKDF2-SHA256، نشست ۱۲ ساعته HttpOnly و محدودیت تلاش ورود.
- تکمیل سابقه مشتری با تماس‌ها، نتایج قدیمی، تعاملات، سفارش‌ها و پیگیری‌ها.
- محاسبه واقعی کیفیت ثبت روزانه و فهرست «مکالمه‌های بدون نتیجه».
- ثبت، اصلاح و آرشیو امن مشتری روی `CustomerIdentity`؛ شماره‌های دیدار و حسابداری حفظ می‌شوند.
- جلوگیری از بازنویسی اطلاعاتی که منبع اصلی آن‌ها `DIGIAHAN` شده است توسط snapshot قدیمی دیدار.
- حذف شماره‌های قدیمی DIDAR از هویت مرکزی هنگام دریافت snapshot جدید دیدار.
- نمایش صریح `STALE` برای دیدار وقتی منبع مخاطبان واقعاً تازه نشده است.

## سرعت و تجربه کاربری

- اصلاح گزارش هفتگی/ماهانه و انتخاب ماه شمسی.
- جایگزینی View سنگین مشتری با ایندکس مستقیم تلفن مرکزی در خلاصه و گزارش روزانه.
- زمان واقعی آزمایش ۳۱روزه: خلاصه ۱۸۶ ms و گزارش روزانه ۲۲۵ ms.
- نمایش درصد پیشرفت بارگذاری گزارش و مرحله جاری Data Gathering.
- جلوگیری از شروع مجدد خودکار گزارش در حالی که بارگذاری قبلی هنوز ادامه دارد.

## نصب و امنیت

- نصب‌کننده v4.3.10 رمز داشبورد مدیریت را دقیقاً یک‌بار دریافت و فقط SHA-256 آن را ذخیره می‌کند.
- میزکار فروش از داشبورد مدیریت مستقل است و با نام کاربری/رمز شخصی وارد می‌شود.
- فایل‌های seller-v2 اکنون داخل Source اصلی هستند و بیلد دیگر به مسیر جانبی payload وابسته نیست.

## کنترل کیفیت

- Build Release: صفر خطا، صفر هشدار.
- Smoke tests v4.3.10: موفق.
- JavaScript syntax check: موفق.
- PowerShell parser: موفق.
- SQL PARSEONLY برای گزارش‌ها و migrationهای اصلی: موفق.
- اجرای خواندنی گزارش‌ها روی دیتابیس واقعی: موفق.

## نکته دیدار

در نسخه عملیاتی هیچ API key یا connector رسمی برای دریافت مخاطبان دیدار وجود ندارد و آخرین منبع ثبت‌شده مربوط به import اولیه است. v4.3.10 این وضعیت را دیگر به‌اشتباه `OK` نشان نمی‌دهد و reconciliation شماره‌ها را درست انجام می‌دهد؛ دریافت زنده مخاطبان فقط پس از ارائه قرارداد/API رسمی دیدار فعال می‌شود و endpoint حدسی در این نسخه ساخته نشده است.
