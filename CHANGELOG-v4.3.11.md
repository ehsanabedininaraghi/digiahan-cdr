# DigiAhan CDR v4.3.11

## تکمیل واقعی کاربران Seller

- صفحه خصوصی `seller-admin` برای ساخت و مدیریت کاربران فروش اضافه شد.
- مدیر می‌تواند نام کاربری، نام فروشنده، شناسه سیستمی، داخلی‌ها و گروه‌های کالا را تعیین کند.
- امکان ویرایش، فعال/غیرفعال‌کردن و تعیین رمز تازه وجود دارد.
- با غیرفعال‌کردن کاربر یا تغییر رمز، همه نشست‌های قبلی او فوراً باطل می‌شوند.
- آخرین ورود و تعداد نشست‌های فعال هر فروشنده در صفحه مدیریت دیده می‌شود.
- پس از ساخت کاربر یا تغییر رمز، متن آماده‌ی تحویل شامل آدرس، نام کاربری و رمز قابل کپی است.
- رمزها قابل بازیابی نیستند و فقط به‌صورت PBKDF2-SHA256 در SQL نگهداری می‌شوند.
- تنظیمات اولیه فایل JSON فقط کاربرِ وجودنداشته را Seed می‌کند؛ پس از آن SQL و صفحه مدیریت منبع اصلی هستند و ری‌استارت تغییرات مدیر را بازنویسی نمی‌کند.
- مسیر و API مدیریت کاربران زیر رمز داشبورد مدیریت محافظت شده‌اند و فروشنده به آن‌ها دسترسی ندارد.

## کنترل کیفیت

- Build Release: صفر خطا و صفر هشدار.
- بررسی نحوی JavaScript و PowerShell.
- تست API ساخت، ورود، تفکیک داخلی، تغییر رمز و غیرفعال‌سازی در بسته نصب.
